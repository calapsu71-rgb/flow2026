name: deploy para coderider

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: setup pages
        uses: actions/configure-pages@v4

      - name: upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."

      - name: deploy to github pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: show deployment url
        run: |
          echo "sitio en produccion en ${{ steps.deployment.outputs.page_url }}"